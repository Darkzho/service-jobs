<div class="container mt-3">
  <p class="mt-3">Estimado <%= @booking.user.first_name %></p>

  <p>La orden fue generada con éxito. Serás contactado en un plazo máximo de 24 horas por el colaborador</p>
  <p>Los detalles de la orden fueron enviados a <%= @booking.user.email %></p>

  <p>Tu número de orden es <%= @booking.id %></p>

  <p>Recuerda que cualquier consulta se hace a través del número de orden.</p>

  <%= link_to "Continuar", booking_status_path(@booking), class: "btn btn-primary me-2" %>
  <%= link_to "Volver a inicio", root_path, class: "btn btn-secondary" %>
</div>